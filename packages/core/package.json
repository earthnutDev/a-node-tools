{
  "type": "module",
  "version": "4.2.16",
  "name": "a-node-tools",
  "private": true,
  "description": "一点点 🤏 node 函数",
  "scripts": {
    "b": "rollup --config rollup.config.js && tsc -p tsconfig.types.json",
    "clean:package": "node scripts/clean-package-json.js",
    "build": "npm run b && npm run clean:package",
    "build:dev": "rollup --config rollup.dev.config.js",
    "dev": "jja rm .eg cls && npm run build:dev &&a_node_tools_dev=all node --unhandled-rejections=strict .eg/index.mjs",
    "push:version": "gvv",
    "push": "gvv",
    "diff": "jja pkg -d",
    "eslint": "jja cls && eslint src",
    "beautify": "npm run eslint && npm run prettier",
    "prettier": "jja cls && prettier . --write",
    "test": "jja clear && jest --coverage",
    "report": "cat ./coverage/lcov.info | coveralls",
    "coverage": "codecov",
    "vjj": "vjj -b",
    "prepublishOnly": "pjj"
  },
  "license": "MIT",
  "dependencies": {
    "@color-pen/static": "^1.0.0",
    "@qqi/log": "^0.1.0",
    "a-js-tools": "^1.0.8",
    "a-type-of-js": "^1.0.5",
    "color-pen": "^2.0.12"
  }
}
